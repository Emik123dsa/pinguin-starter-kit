/**
 * Provide environment variables.
 *
 * @export
 * @enum {number}
 */
export enum Environment {
  Development = 'development',
  Production = 'production',
  Staging = 'staging',
  Test = 'test',
  Provision = 'provision',
  Default = 'default',
}
