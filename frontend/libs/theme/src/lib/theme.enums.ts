/**
 * Theme color types.
 *
 * @export
 * @enum {number}
 */
export enum ThemeColorTypes {
  Dark = 'dark-theme',
  Light = 'light-theme',
}

/**
 * Color scheme types.
 *
 * @export
 * @enum {number}
 */
export enum ColorSchemeTypes {
  Dark = 'dark-scheme',
  Light = 'light-scheme',
}
